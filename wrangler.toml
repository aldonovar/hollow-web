name = "hollow-web"
compatibility_date = "2024-05-01"
pages_build_output_dir = "dist"

[vars]
ENVIRONMENT = "production"
# Las variables de Supabase se inyectan desde el dashboard de Cloudflare, 
# no se exponen aquí por seguridad.
