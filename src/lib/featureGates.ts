import {
  formatLimit,
  getFlags,
  getLimits,
  getRequiredTierName,
  hasFeature,
  isWithinQuota,
  meetsMinimumTier,
  resolveTier,
} from '@hollowbits/core';

export type { FeatureFlags, FeatureGate, Tier, TierLimits } from '@hollowbits/core';

export {
  formatLimit,
  getFlags,
  getLimits,
  getRequiredTierName,
  hasFeature,
  isWithinQuota,
  meetsMinimumTier,
  resolveTier,
};
